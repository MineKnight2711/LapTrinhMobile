name: keyboard_mobile_app
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.2 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  firebase_core: ^2.9.0
  firebase_auth: ^4.4.1
  firebase_storage: ^11.1.0
  fluttertoast: ^8.2.1
  cloud_firestore: ^4.5.0
  shared_preferences: ^2.1.0
  google_fonts: ^4.0.3
  google_sign_in: ^6.1.0
  flutter_staggered_grid_view: ^0.4.0
  get: ^4.6.5
  http: ^0.13.6
  intl: ^0.18.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/icons/

